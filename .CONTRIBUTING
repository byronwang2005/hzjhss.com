# 贡献说明

本仓库为内部资产，仅用于安全审计相关开源场景。未经授权，不得用于生产发布、商业分发或无关项目复用。

## 使用范围

- 仅限内部安全审计、审计复现、合规检查与必要的开源披露。
- 不应提交敏感信息、凭据、内部地址、客户数据或未脱敏材料。
- 涉及外部公开前，应完成安全审查与授权确认。

## 本地预览

直接用浏览器打开 `index.html`，或使用任意静态文件服务器预览。

## 修改要求

- 保持页面内容简洁、准确，避免引入无关功能。
- 保留现有中文文案风格，除非修改目标明确要求重写。
- HTML 保持语义化和可访问性。
- CSS 仅围绕当前静态页面结构调整。
- 不提交生成文件、本地编辑器配置、依赖目录或环境文件。

## 提交前检查

1. 在桌面和移动宽度预览页面。
2. 确认链接、代码片段和公司标识显示正常。
3. 运行 `git status --short`，确认只包含预期变更。
